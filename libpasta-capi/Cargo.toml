[package]
name = "pasta"
version = "0.1.0-rc1"
authors = ["Sam Scott <sam.scott89@gmail.com>"]
build = "build.rs"


[dependencies]
libc = "0.2"
libpasta = { version = "0.1.0-rc1", path = ".." }
rpassword = "2.0"

[build-dependencies]
cbindgen = "0.5"

[lib]
crate-type = ["cdylib", "staticlib"]