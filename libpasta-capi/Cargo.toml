[package]
name = "pasta"
version = "0.1.0"
authors = ["Sam Scott <sam.scott89@gmail.com>"]
build = "build.rs"


[dependencies]
libc = "0.2"
libpasta = { version = "0.1.0", path = ".." }
rpassword = "5.0.0"

[build-dependencies]
cbindgen = "0.15.0"

[lib]
crate-type = ["cdylib", "staticlib"]
